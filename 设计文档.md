# 个人网站（展示工作经历）设计文档

> 版本：v0.1（设计稿，未实现）
> 定位：公开求职站，3 分钟让面试官 / 猎头看懂你的核心竞争力。
> 与「模块化个人工作台」**独立并存**——工作台是私用工具，本站是公开门面，技术栈不同、分别维护。

---

## 1. 定位与目标

| 维度 | 说明 |
| --- | --- |
| 受众 | 面试官、猎头、技术负责人 |
| 目标 | 快速建立专业印象，突出差异化竞争力（信创 / 达梦迁移方向） |
| 形态 | 纯静态网站，GitHub Pages / Netlify 免费托管 |
| 与工作台关系 | 独立项目，并存 |

---

## 2. 技术栈

| 项 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | **Astro 5** | 内容站最优：静态生成、默认零 JS、加载快、SEO 好 |
| 样式 | **Tailwind CSS** | 原子化、快、易维护 |
| 内容 | **Astro Content Collections (MDX)** | 经历 / 项目结构化、类型安全、易维护 |
| 部署 | GitHub Pages（Actions CI）或 Netlify | 免费、自动构建 |
| 可选 | `@astrojs/i18n` | 若需中英双语 |

**不用后端、不用数据库**——纯静态足以，维护成本最低。

---

## 3. 信息架构

单页长滚动 + 顶部锚点导航（移动端汉堡菜单）。若做博客，博客为独立路由 `/blog`。

区块顺序：
1. **Hero**（首屏）：姓名 / 一句话定位 / 核心技能标签 / CTA（看简历、联系）
2. **About**（关于我）：自我介绍、技术栈、当前方向
3. **Experience**（工作经历 · 时间线）：按时间倒序
4. **Projects**（精选项目）：2-4 个代表性
5. **Skills**（技能）：按语言 / 框架 / 数据库 / 中间件分组
6. **Blog**（可选）
7. **Contact**（联系 / 简历下载）

---

## 4. 内容模型（Content Collections）

`src/content/` 下用 schema 约束内容，新增经历 / 项目只需加一个 `.md` 文件。

```ts
// src/content/config.ts
import { defineCollection, z } from 'astro:content'

const experiences = defineCollection({
  type: 'content',
  schema: z.object({
    company: z.string(),        // 可脱敏：写「某能源央企」
    role: z.string(),
    start: z.coerce.date(),
    end: z.union([z.coerce.date(), z.literal('present')]),
    location: z.string().optional(),
    summary: z.string(),
    highlights: z.array(z.string()),   // 量化成果
    tech: z.array(z.string()),
    confidential: z.boolean().default(true), // 是否脱敏项目
    order: z.number(),
  }),
})

const projects = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    summary: z.string(),
    role: z.string(),
    challenge: z.string(),
    solution: z.string(),
    result: z.string(),         // 量化结果
    tech: z.array(z.string()),
    links: z.object({ repo: z.string().optional(), demo: z.string().optional() }).optional(),
    confidential: z.boolean().default(true),
    featured: z.boolean().default(false),
    order: z.number(),
  }),
})

export const collections = { experiences, projects }
```

---

## 5. 页面与组件

| 文件 | 职责 |
| --- | --- |
| `Layout.astro` | 导航 + 页脚 + SEO meta + 暗色模式切换 + 容器 |
| `Hero.astro` | 姓名 / 定位 / 核心标签 / CTA |
| `About.astro` | 自我介绍 |
| `ExperienceTimeline.astro` | 纵向时间线，按 `order` 渲染 |
| `ProjectCard.astro` + `Projects.astro` | 精选项目卡片 |
| `Skills.astro` | 分组技能 |
| `Contact.astro` | 邮箱 / GitHub / 微信 + 简历 PDF 下载 |

---

## 6. 视觉风格

- 简洁专业、克制配色（主色 1 个 + 中性灰阶）
- 响应式（移动优先）
- 暗色 / 亮色切换（`prefers-color-scheme` + 手动）
- 打印友好：整页或简历页可打印为 PDF
- 动效克制（滚动淡入即可，不过度）

---

## 7. 内容脱敏规范（必读）

公开站不能泄露雇主机密。规则：

- **公司名**：用行业 + 性质代称（「某能源央企」「某信创厂商」），不写真名
- **不写**内部架构图、未公开代码、客户数据、内部系统名
- **聚焦**：技术难点、你的方案、可量化结果
- **信创 / 达梦迁移**经历是核心竞争力，重点写技术深度，**不写具体客户名与内部系统名**
- `confidential: true` 的内容在源码里也只用脱敏表述

> 你后续提供的真实经历，我会按此规范落到 `src/content/`，源码中始终以脱敏文案呈现。

---

## 8. 部署

**GitHub Pages（推荐，免费）：**
- 仓库 `username.github.io` 或项目页
- `.github/workflows/deploy.yml`：push 自动 `astro build` → 发布
- 自定义域名可选

**Netlify / Vercel：** 连仓库，build `astro build`，输出 `dist/`。

---

## 9. SEO 与性能

- 每页 `title` / `description` / `og` 标签
- 语义化 HTML、`alt` 文本
- Astro 默认高性能（零 JS、按需 hydrate）
- 可选 sitemap + RSS（若做博客）

---

## 10. 目标目录结构

```
personal-site/
├── astro.config.mjs
├── tailwind.config.ts
├── package.json
├── .github/workflows/deploy.yml   # GitHub Pages CI
├── src/
│   ├── content/
│   │   ├── config.ts              # 集合 schema
│   │   ├── experiences/*.md       # 每段经历一篇
│   │   └── projects/*.md          # 每个项目一篇
│   ├── layouts/Layout.astro
│   ├── components/                # Hero/About/ExperienceTimeline/ProjectCard/Skills/Contact
│   ├── pages/
│   │   ├── index.astro            # 单页
│   │   └── blog/                  # 可选
│   └── styles/
└── public/                        # 简历 PDF、头像、favicon
```

---

## 11. 后续步骤

1. **你提供真实经历要点**（公司性质 / 角色 / 时间 / 量化成果），我按脱敏规范落到 `content/`
2. 确认视觉偏好（主色 / 暗色默认）
3. 我搭骨架（Astro 初始化 + 组件 + 样式）
4. 填内容 → 本地预览 → 部署

> 注：模块化工作台的设计文档仍保留在 `C:/Users/18496/WorkBuddy/modular-workspace/设计文档.md`，其 4 项决策（后端栈 / 首个模块 / 本地优先程度 / 形态）待你后续拍板，与本个人站互不影响。
